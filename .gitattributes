# Normalize line endings to LF in the repo for cross-platform consistency
# (Docker images and CI run on Linux).
* text=auto eol=lf

*.png binary
*.jpg binary
*.gif binary
*.parquet binary
*.ico binary
